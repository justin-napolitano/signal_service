###############################################
# 🌐 GENERAL ENVIRONMENT
###############################################
TZ=America/New_York
LOG_LEVEL=INFO

###############################################
# 📱 SIGNAL CONFIGURATION
###############################################
# The Signal phone number linked to signal-cli-rest-api
SIGNAL_NUMBER=+1XXXXXXXXXX

# Run mode for signal-cli-rest-api:
#   native = HTTP long-poll receive (recommended)
#   jsonrpc = WebSocket/JSON-RPC mode
SIGNAL_API_MODE=native

# Directory where signal-cli stores its link data
SIGNAL_DATA_DIR=./signal-cli

# Base URL for internal signal-cli REST API
SIGNAL_API_BASE=http://signal-api:8080


###############################################
# 🔐 AUTH TOKENS
###############################################
# Shared secret for /notify endpoint in notifier-gateway
GATEWAY_TOKEN=replace_with_long_random_hex

# Shared secret for gateway → assistant inbox (if different)
# If blank, assistant will reuse GATEWAY_TOKEN
INBOX_TOKEN=

###############################################
# 🤖 ASSISTANT-CORE CONFIG
###############################################
# Disable or enable retrieval-augmented generation
DISABLE_RAG=true
RAG_DATA_DIR=/data

# OpenAI API credentials (optional)
OPENAI_API_KEY=sk-your-openai-key
OPENAI_MODEL=gpt-4o-mini

# List of allowed senders (comma separated Signal numbers)
# Leave blank to allow all (useful for local dev)
ALLOW_SENDERS=+1XXXXXXXXXX

# Long-poll timeout for receive loop (seconds)
RECEIVE_TIMEOUT=60

# Where assistant expects inbound messages
INBOX_URL=http://assistant-core:8088/inbox


###############################################
# 📨 NOTIFIER-GATEWAY CONFIG
###############################################
# HTTP endpoint used by all services to send Signal messages
NOTIFY_URL=http://notifier-gateway:8787/notify

# Host port exposed for the gateway (optional)
GATEWAY_PORT=8787


###############################################
# ☀️ WEATHER-SERVICE DEFAULTS
###############################################
CITY=Orlando
STATE=FL
# When to send morning weather push (cron syntax)
CRON_SCHEDULE=0 7 * * *


###############################################
# 🧭 OTHER / FUTURE SERVICE VARIABLES
###############################################
# add service-specific configs here (e.g., metrics, jira)
